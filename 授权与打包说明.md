# 授权（机器码锁定）与桌面打包说明

本软件已内置**离线一机一码授权**与**桌面化启动器**。本文说明授权原理、供应商（你）如何发放激活码、以及如何打包成 Windows 安装包。

---

## 一、授权方案原理

采用 **Ed25519 非对称签名 + 硬件指纹** 的离线节点锁定，无需联网服务器：

```
你（开发者）保管：私钥 tools/keygen_private.txt + keygen 工具   ← 绝不分发
软件内置：       公钥 license/pubkey.txt                       ← 随软件发布（公开也安全）

激活流程：
 1. 客户安装并启动 → 显示激活遮罩（应用被锁定，所有计算接口拒绝服务）
 2. 软件采集硬件指纹 → 机器码 = SHA256(主板UUID | MachineGuid | CPU_ID) 前 24 位
 3. 客户把机器码发给你
 4. 你运行 keygen 对机器码签名 → 得到激活码
 5. 客户输入激活码 → 软件用公钥验签通过 → 写入许可文件、解锁
 6. 每次启动都用「当前机器」重新算机器码再验签
```

**为什么只能一台电脑用**：激活码是对该机机器码的数字签名。把整个安装目录或许可文件复制到另一台电脑，机器码会变，验签失败 → 锁定。私钥只在你手里，别人无法伪造激活码（内置的是公钥，只能验签不能签发）。

许可文件位置：`%LOCALAPPDATA%\GeoDetectorPro\license.dat`（用户可写，便于装在 Program Files）。

> 关于「只能安装一次」：安装包本身可被复制，无法从技术上阻止复制安装；真正的强制点是**运行时验签**——没有你签发的、与本机绑定的激活码，软件就用不了。一个激活码 = 一台机器。每售出一台机器发一个激活码即可。

---

## 二、发放激活码（供应商日常操作）

1. 客户在激活页点机器码复制，发给你，例如：`CC39-CE72-0E19-D69C-EF02-87CA`
2. 你在项目目录运行 keygen：

```bat
"D:\Program Files\R\R-4.5.2\bin\Rscript.exe" tools\keygen.R CC39CE720E19D69CEF0287CA
```

   （机器码带不带分隔符都行）输出形如：

```
激活码 : 5242079d-b4324cf5-95fda984-...-fcc5fa0b
```

3. 把激活码发给客户，客户粘贴进激活框点「激活」即可。

> 首次使用前需生成密钥对（只做一次）：
> ```bat
> "D:\...\Rscript.exe" tools\gen_keys.R
> ```
> 会生成 `tools\keygen_private.txt`（私钥，保密！已在 .gitignore）和 `license\pubkey.txt`（公钥，随软件发布）。
> **务必把 `keygen_private.txt` 离线备份好**——丢了就无法再为新客户签发激活码（且不能重建，否则旧激活码全失效）。

---

## 三、打包成 Windows 桌面安装包

### 桌面运行形态
`launcher\地理探测器.vbs` 会：隐藏窗口启动 R 后端 → 等服务就绪 → 用 Edge/Chrome 的「应用窗口」模式（无地址栏，像独立软件）打开界面 → 关闭窗口后后端由心跳看门狗自动退出。

### 打包步骤

**1. 准备便携 R（让客户机器无需装 R）**
- 下载 R 4.5.x，安装到临时目录，或用 [R-Portable](https://sourceforge.net/projects/rportable/)。
- 把整个 R 复制为项目根下的 `R-portable\`（需有 `R-portable\bin\Rscript.exe`）。
- 在该便携 R 中预装依赖：
  ```bat
  R-portable\bin\Rscript.exe -e "install.packages(c('plumber','GD','readxl','jsonlite','car','callr','later','httpuv','sodium'), repos='https://mirrors.tuna.tsinghua.edu.cn/CRAN/')"
  ```
- 可选瘦身：删除 `R-portable\library` 下用不到的大包文档/demo 以减小体积。

**2. 用 Inno Setup 编译安装包**
- 安装 [Inno Setup 6](https://jrsoftware.org/isinfo.php)。
- 用 Inno Setup 打开 `installer\setup.iss`，点 **Build**（或命令行 `ISCC installer\setup.iss`）。
- 产物：`installer\Output\地理探测器_安装包_v1.0.0.exe`。

安装包会：装到 `Program Files\GeoDetectorPro`，建开始菜单 + 桌面快捷方式（指向启动器），并**自动排除 `tools\`（私钥+keygen）**。

### 不打便携 R 的简化方案
如果只在你自己/少数装了 R 的电脑用：跳过便携 R，启动器会回退到本机已安装的 R（`launcher\地理探测器.vbs` 里的回退路径）。此时安装包可不含 R-portable（注释掉 setup.iss 中对应的 Files 行）。

---

## 四、测试 / 重置激活状态

- 删除 `%LOCALAPPDATA%\GeoDetectorPro\license.dat` 即可回到「未激活」状态重新测试。
- 当前这台开发机的机器码与激活码已在开发中验证通过。

## 五、安全边界（务实说明）

- 本方案防住了「复制到别的电脑直接用」「普通用户伪造激活码」。
- 它**不是**防破解的铜墙铁壁：源码是 R/JS 明文，有能力者可改代码绕过验签。若需更强保护，可（后续）对 R 代码做字节码编译/混淆、或将验签逻辑下沉到编译型组件。对绝大多数商用分发场景，当前强度已足够。
